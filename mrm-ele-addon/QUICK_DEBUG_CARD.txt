╔══════════════════════════════════════════════════════════════════════════╗
║                 MRM CF7 POPUP - QUICK DEBUG CARD                         ║
║                      (Print Karke Rakh Lo!)                              ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 1: CHECK BROWSER CONSOLE (MOST IMPORTANT!)                        │
└─────────────────────────────────────────────────────────────────────────┘

Press: F12 → Console Tab → Submit Form → Check Messages

✅ SUCCESS:
   "Data sent to Google Sheets successfully"

❌ ERRORS & FIXES:
   ┌──────────────────────────────────────────────────────────────────┐
   │ Error                          │ Fix                            │
   ├──────────────────────────────────────────────────────────────────┤
   │ API request failed: 400        │ Sheet ID/API key check karo    │
   │ API request failed: 401        │ API key invalid - new banao    │
   │ API request failed: 403        │ Sheet public karo              │
   │ API request failed: 404        │ Sheet ID galat hai             │
   │ Security check failed          │ Ctrl+F5 (hard refresh)         │
   │ Rate limit exceeded            │ 5 minutes wait karo            │
   └──────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 2: CHECK NETWORK TAB (DETAILED INFO)                              │
└─────────────────────────────────────────────────────────────────────────┘

F12 → Network Tab → Submit → Filter: "mrm_cf7_popup" → Click Request

Check:
  □ Status Code = 200 ✅
  □ Request has sheet_id, api_key, data
  □ Response has "success": true

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 3: VERIFY SETTINGS                                                │
└─────────────────────────────────────────────────────────────────────────┘

Widget Settings:
  □ Google Sheets Integration = ON
  □ Sheet ID filled (from URL)
  □ Sheet Name correct (default: Sheet1)
  □ API Key filled (from Google Cloud)
  □ Field Mapping valid JSON

Google Sheet:
  □ Share → "Anyone with the link"
  □ Permission: Viewer
  □ Column names match mapping

CF7 Form:
  □ Form working without widget?
  □ Field names in mapping correct?

┌─────────────────────────────────────────────────────────────────────────┐
│ LOGS LOCATION QUICK REFERENCE                                           │
└─────────────────────────────────────────────────────────────────────────┘

1. Browser Console      → F12 → Console
2. Network Tab          → F12 → Network
3. WordPress Debug Log  → /wp-content/debug.log
4. Security Logs        → Database: wp_mrm_cf7_popup_security_logs
5. Server Error Log     → /public_html/error_log

┌─────────────────────────────────────────────────────────────────────────┐
│ COMMON FIXES (90% PROBLEMS)                                             │
└─────────────────────────────────────────────────────────────────────────┘

Problem: Sheet me data nahi ja raha
Fix 1:  Console check karo → Error dekho → Wo fix karo
Fix 2:  Sheet public hai? → Share settings check karo
Fix 3:  API key valid? → Google Cloud Console me verify karo
Fix 4:  Field mapping sahi? → jsonlint.com pe validate karo
Fix 5:  Column names match? → Sheet aur mapping compare karo

Problem: "Security check failed"
Fix:    Ctrl + F5 (hard refresh page)

Problem: "Rate limit exceeded"
Fix:    5 minutes wait karo ya cookies clear karo

Problem: Form submit nahi ho raha
Fix:    CF7 plugin active? Form properly configured?

┌─────────────────────────────────────────────────────────────────────────┐
│ FIELD MAPPING TEMPLATE                                                  │
└─────────────────────────────────────────────────────────────────────────┘

Correct Format:
{
  "your-name": "Name",
  "your-email": "Email",
  "your-phone": "Phone",
  "your-message": "Message"
}

Rules:
• Left side  = CF7 field name (from form code)
• Right side = Sheet column name (exact match, case-sensitive)
• Use double quotes everywhere
• Comma after each line (except last)
• Validate at: jsonlint.com

┌─────────────────────────────────────────────────────────────────────────┐
│ GOOGLE SHEETS SETUP CHECKLIST                                           │
└─────────────────────────────────────────────────────────────────────────┘

Sheet Setup:
  □ First row = Column headers (Name, Email, etc.)
  □ Share → "Anyone with the link"
  □ Permission: Viewer (minimum)
  □ Copy Sheet ID from URL:
    https://docs.google.com/spreadsheets/d/SHEET_ID_HERE/edit
                                            ↑↑↑↑↑↑↑↑↑

API Key Setup:
  □ Google Cloud Console → Create Project
  □ Enable "Google Sheets API"
  □ Create Credentials → API Key
  □ Copy API key
  □ (Optional) Restrict key to Google Sheets API only

┌─────────────────────────────────────────────────────────────────────────┐
│ TESTING SEQUENCE                                                        │
└─────────────────────────────────────────────────────────────────────────┘

1. Test CF7 form alone (without Google Sheets)
   → Working? ✅ Continue ❌ Fix CF7 first

2. Enable Google Sheets + Submit form
   → Console me success? ✅ Continue ❌ Fix error

3. Check Google Sheet
   → Row added? ✅ SUCCESS! ❌ Check permissions

4. Test with multiple submissions
   → All working? ✅ DONE! ❌ Check rate limits

┌─────────────────────────────────────────────────────────────────────────┐
│ ENABLE DEBUG MODE                                                       │
└─────────────────────────────────────────────────────────────────────────┘

Edit: wp-config.php (above "That's all, stop editing!")

Add:
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);

View logs: /wp-content/debug.log

┌─────────────────────────────────────────────────────────────────────────┐
│ BROWSER CONSOLE DIAGNOSTIC SCRIPT                                      │
└─────────────────────────────────────────────────────────────────────────┘

Paste in Console:

// Quick Check
console.log('Widgets:', jQuery('.mrm-cf7-popup-wrapper').length);
console.log('Config:', jQuery('.mrm-cf7-popup-modal').data('google-sheets'));
console.log('AJAX URL:', mrmCF7PopupData.ajaxUrl);
console.log('Nonce:', mrmCF7PopupData.nonce ? 'Present' : 'Missing');

┌─────────────────────────────────────────────────────────────────────────┐
│ DATABASE QUERY (phpMyAdmin)                                             │
└─────────────────────────────────────────────────────────────────────────┘

Check Security Logs:
SELECT * FROM wp_mrm_cf7_popup_security_logs 
ORDER BY timestamp DESC LIMIT 20;

Check Rate Limits:
SELECT * FROM wp_mrm_cf7_popup_security_logs 
WHERE type = 'Rate Limit Exceeded' 
ORDER BY timestamp DESC;

┌─────────────────────────────────────────────────────────────────────────┐
│ WHEN TO CHECK WHICH LOG                                                │
└─────────────────────────────────────────────────────────────────────────┘

Form nahi submit ho raha       → Console (JS errors)
Sheet me data nahi ja raha      → Console + Network Tab
API error                       → Network Tab Response
Security block                  → Database Security Logs
PHP error                       → WordPress Debug Log
Server crash                    → Server Error Log

┌─────────────────────────────────────────────────────────────────────────┐
│ CONTACT SUPPORT WITH                                                    │
└─────────────────────────────────────────────────────────────────────────┘

Required Screenshots:
  □ Browser Console (with error)
  □ Network Tab (Request + Response)
  □ Widget Settings (Google Sheets section)
  □ Google Sheet (column headers)
  □ CF7 Form structure
  □ Debug log entries (last 50 lines)

┌─────────────────────────────────────────────────────────────────────────┐
│ PRO TIPS                                                                │
└─────────────────────────────────────────────────────────────────────────┘

1. Always check Console FIRST!
2. Use Incognito mode for testing (no cache)
3. Validate JSON at jsonlint.com before pasting
4. Test API key separately in browser
5. Keep Sheet simple during testing (2-3 columns only)
6. Match column names EXACTLY (case-sensitive!)
7. Wait 5 minutes if rate limited
8. Hard refresh (Ctrl+F5) if "Security check failed"

┌─────────────────────────────────────────────────────────────────────────┐
│ MOST COMMON MISTAKES                                                    │
└─────────────────────────────────────────────────────────────────────────┘

❌ Sheet not publicly accessible
❌ API key has extra spaces or missing characters
❌ Column names don't match (Name vs name)
❌ Field mapping JSON invalid (missing quotes/commas)
❌ CF7 field names wrong in mapping
❌ Sheet ID copied incorrectly (has /edit or other text)
❌ Using old/disabled API key
❌ Sheet deleted or moved

┌─────────────────────────────────────────────────────────────────────────┐
│ SUCCESS INDICATORS                                                      │
└─────────────────────────────────────────────────────────────────────────┘

✅ Console: "Data sent to Google Sheets successfully"
✅ Network Tab: Status 200
✅ Response: {"success": true, ...}
✅ Google Sheet: New row appears with timestamp
✅ No errors in debug.log

═══════════════════════════════════════════════════════════════════════════

Version: 1.0.0 | Last Updated: Dec 6, 2024
Plugin: MRM Ele Addon | Widget: CF7 Popup
Documentation: CF7_POPUP_DOCUMENTATION.md

═══════════════════════════════════════════════════════════════════════════

REMEMBER: 90% problems = Console errors
          Just open F12 and check! 🎯

═══════════════════════════════════════════════════════════════════════════
